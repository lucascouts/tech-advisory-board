[project]
name = "logfetch"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1",
    "httpx>=0.27",
    "rich>=13.7",
]

[project.scripts]
logfetch = "logfetch.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
